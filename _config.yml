# GitHub Pages configuration
# This helps ensure proper MIME types are served

# Enable Jekyll processing
include:
  - .nojekyll

# Markdown processor
markdown: kramdown

# Build settings
safe: false
